{
  "$schema": "https://schemas.wp.org/trunk/block.json",
  "apiVersion": 3,
  "name": "eventmesh/provider-embed",
  "title": "EventMesh provider embed",
  "category": "widgets",
  "textdomain": "eventmesh",
  "icon": "format-audio",
  "description": "A compact embedded player (Spotify, SoundCloud, or Mixcloud) for the current event, when a matching provider link was found and cached during sync. Rendered directly in PHP, not via Block Bindings.",
  "keywords": ["spotify", "soundcloud", "mixcloud", "player", "eventmesh"],
  "usesContext": ["postId"],
  "supports": {
    "html": false,
    "color": {
      "background": true
    },
    "spacing": {
      "margin": true,
      "padding": true
    },
    "border": {
      "color": true,
      "radius": true,
      "style": true,
      "width": true
    }
  },
  "editorScript": "file:./index.js"
}
